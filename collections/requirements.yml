---
collections:
  - name: kubernetes.core
    version: ">=2.4.0"
  - name: rhoai.ai_workspace
    # For local development, install from source:
    #   ansible-galaxy collection build ../rhoai-ai-workspace-collection
    #   ansible-galaxy collection install rhoai-ai_workspace-1.0.0.tar.gz
    #
    # For production, publish to Automation Hub or Private Automation Hub
    # and reference the version here:
    version: ">=1.0.0"
    source: https://your-automation-hub.example.com/api/galaxy/
